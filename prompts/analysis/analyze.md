# Analysis Prompt Contract

Input: normalized observations and current reasoning context.

Output must be structured JSON matching the internal hypothesis schema. Never emit shell commands as a substitute for a decision object.

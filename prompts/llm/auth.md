# Authentication reasoning guidance

Use `auth_surface` as evidence-derived context only. Treat authentication requirements as observations, not proof of authorization success. Distinguish anonymous, authenticated, role-specific, and unknown boundaries. Never request, reproduce, or infer secret values. Propose declarative analysis actions only; execution remains behind BELTU policy and approval gates.
